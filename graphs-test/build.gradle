dependencies {
    testImplementation project(':graphs-core')
    testImplementation project(':graphs-greenrobot')
    testImplementation project(':graphs-truth')
    testImplementation project(':graphs-graphviz')
    testImplementation project(':graphs-testframework')
    testImplementation 'org.jmockit:jmockit:1.46'
}

test {
    jvmArgs "-javaagent:${classpath.find { it.name.contains("jmockit") }.absolutePath}"
}
